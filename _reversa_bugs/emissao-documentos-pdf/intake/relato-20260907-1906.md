# Relato 2026-09-07 19:06

> Fonte: chat (Adelino), 2026-09-07 19:06 -03:00
> Contexto: `emissao-documentos-pdf`

## Fala do usuário (verbatim)

> "deu erro ao gerar declaração de vínculo e declaração de matrícula... deu erro -> Erro ao gerar documento: doc.save is not a function"

## Observações do registrador

- Defeito único: dois tipos de documento (Declaração de Matrícula e Declaração de Vínculo) falham
  na tela "Gerar Documento" com o toast `Erro ao gerar documento: doc.save is not a function`.
- Suspeita imediata: geradores `generateDeclaracaoPDF` e `generateDeclaracaoVinculoPDF` em
  `src/lib/pdf-service.ts` são `async` (linhas 358 e 456), mas as chamadas em
  `src/components/Tabs/DocumentosTab.ts` (linhas 134, 137 e 154) não têm `await`; `doc` vira uma
  `Promise` e `downloadPDF` chama `doc.save` sobre ela.
- Histórico e Boletim (linhas 144 e 150) já usam `await` e funcionam — consistente com o relato de
  que apenas os dois tipos de Declaração quebram.
- Sem anexos fornecidos pelo usuário (nenhum print).